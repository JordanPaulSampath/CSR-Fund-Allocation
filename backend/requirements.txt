fastapi==0.115.2
uvicorn[standard]==0.31.1
pydantic==2.9.2
pulp==2.9.0
PyYAML==6.0.2
python-multipart==0.0.12
bcrypt==4.2.0
python-dotenv==1.0.1
SQLAlchemy==2.0.35
# Postgres driver — used only when DATABASE_URL points at Supabase/Postgres.
# Harmless (and wheels-only) when running the local SQLite fallback.
psycopg2-binary==2.9.9
